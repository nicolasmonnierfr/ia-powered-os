# === Dépendances Python du projet IA-Powered-OS ===
# Installation : voir bootstrap/setup-windows.ps1
#
# IMPORTANT (CPU sans GPU NVIDIA) :
# torch doit être installé depuis l'index CPU AVANT whisperx, sinon pip
# tente de tirer une version CUDA inutile. Le script bootstrap gère cet ordre.
# Commande de référence pour torch CPU :
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

whisperx
python-dotenv

# torch / torchaudio sont installés séparément par le script bootstrap
# (index CPU dédié) pour éviter de tirer les paquets CUDA.
